<section>
	<div class="row">
		<div class="col-sm-10 col-sm-offset-1">
			<p>Branch</p>
			<table class="table">
				<% @semester.each do |sem| %>
				<tr>
					<td><%= sem.id%></td>
					<td><%= sem.description %></td>
				</tr>
				<% end %>
			</table>		
		</div>
	</div>>
</section>